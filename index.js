const features = require('./config');

if (features.Albums) {
  // Enable Albums feature
  console.log('Albums feature is enabled!');
}
if (features.Chats) {
  // Enable Chats feature
  console.log('Chats feature is enabled!');
}
if (features.Avatar) {
  // Enable Avatar feature
  console.log('Avatar feature is enabled!');
}